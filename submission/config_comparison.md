| Distro        | Package Manager       | Init System | SSH Service Name | Firewall                 |

| ------------- | --------------------- | ----------- | ---------------- | ------------------------ |

| Ubuntu/Debian | `apt` / `dpkg`        | `systemd`   | `ssh`            | `ufw` / `iptables`       |

| Kali Linux    | `apt` / `dpkg`        | `systemd`   | `ssh`            | `ufw` / `iptables`       |

| RedHat        | `yum` / `dnf` / `rpm` | `systemd`   | `sshd`           | `firewalld` / `iptables` |

| CentOS        | `yum` / `dnf` / `rpm` | `systemd`   | `sshd`           | `firewalld` / `iptables` |

